#服务器端口
server.socket.port=8888

#缓存容量最小值
server.cache.size.minimum=16
#缓存容量默认值
server.cache.size.default=4096

#每个实体类持久化线程池大小（默认为1）
#server.persistence.pool.size=1

#事件处理线程池大小（默认为cpu的数量）
#server.event.pool.size=0

##---------------------------
#平台标识（由数字和字母组成）
server.config.platform=test

#服标识（多个使用,分割）
server.config.server=1
